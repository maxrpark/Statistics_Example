import { Chart, PageTitle, StatSection } from "../components";

const Profile: React.FC = () => {
  return (
    <main className='section-center'>
      <PageTitle />
      <StatSection />
      <Chart />
    </main>
  );
};

export default Profile;
